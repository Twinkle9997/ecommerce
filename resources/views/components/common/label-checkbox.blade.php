<div class="d-flex gap-2 mt-1">
    <input type="{{ $type }}" class="my-auto" name="{{ $name }}" id="{{ $name }}"
        value="{{ $value }}" />
    <label for="{{ $name }}" class="capitalize my-auto"> {{ $label }} </label>
</div>
